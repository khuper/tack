feat: context architecture, drift UX, handoff compaction, YAML detectors, MCP

L) Context architecture (domain-scoped, proof-carrying)
- files.ts: implementationStatusPath(), contextIndexPath(); contextTemplates()
  extended with implementation_status.md and context_index.md
- signals.ts: ImplementationStatus, ImplementationStatusEntry; ContextPack and
  HandoffReport.implementation_status; log event compaction:archive_handoffs
- contextPack.ts: parseImplementationStatusFile(); parseContextPack() returns
  implementation_status
- handoff.ts: report includes implementation_status; markdown "## 3) Implementation
  Status"; "## Agent Priorities" after Summary (use .tack/ as canonical context,
  prefer spec/audit/drift/implementation_status for architecture, no new business
  systems without spec + decision, repair .tack/ if stale)
- compaction.ts: archiveOldHandoffs(keepRecent), stem-based grouping,
  handoffs/archive/; generateHandoff() calls archiveOldHandoffs(10)
- mcp.ts: read-only resources tack://context/intent|facts|handoff/latest|
  decisions_recent|machine_state
- package.json: @modelcontextprotocol/sdk; build copies src/detectors/rules to
  dist/detectors/rules

M) Drift UX (approve / deny / table + auto-resolve)
- DriftAlert.tsx: Deny option (forbidden_systems, resolve rejected)
- Watch.tsx: alertable includes undeclared_system
- computeDrift.ts: reconcile step; mark unresolved items rejected when
  fingerprint no longer in current diff so drift count drops when cause removed

N) Framework and auth detectors moved to YAML
- src/detectors/rules/framework.yaml, auth.yaml
- yamlRunner.ts: createDetectorFromYaml(), getRulesDir(); binary detection,
  safe regex, validation
- index.ts: PRIMARY_DETECTORS uses YAML for framework and auth; rest remain TS
- tests/detectors/yamlRunner.test.ts; build step copies rules to dist
